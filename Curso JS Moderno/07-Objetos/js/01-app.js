const nombre = "Monitor 20 pulgadas";
const precio = 300;
const stock = true;


// Un objeto agrupa en una variable toda la información, independientemente del tipo.

// Objeto literar
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    stock: true
}