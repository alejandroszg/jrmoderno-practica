const producto = {
    nombre: "Monitor 24 pulgadas",
    precio: 300,
    disponible: true
}

// Destructuring
const { nombre, precio, disponible } = producto;
console.log(nombre);
console.log(precio);
console.log(disponible)