const producto = 'Monitor 20 Pulgadas';

// .toUpperCase para convertir un string en mayúsculas
console.log(producto.toUpperCase());
// .toLowerCase para convertir un string en minúsculas
console.log(producto.toLowerCase());