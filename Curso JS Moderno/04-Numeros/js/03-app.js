let resultado;

// PI
resultado = Math.PI;

// .round redondea | Importante: considera .5 un número para redondear hacia arriba.
resultado = Math.round (2.5);

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.8);

// Raíz cuadrada
resultado = Math.sqrt(144);

// Absoluto
resultado = Math.abs(-230);

// Potencia
resultado = Math.pow(8, 3);

// Mínimo
resultado = Math.min(2,3,5,2)

// Máximo
resultado = Math.max(1,2,3,2323);

// Aleatorio: el método .random() genera un número aleatorio entre 0 y 1
resultado = Math.random();
resultado = Math.ceil(Math.random() * 10);


console.log(resultado); 