//Ciclo forEach. El forEach recibe 2 parámetros: el elemento del array y su index.

const pendientes = ['Trabajar', 'Alimentación', 'Ejercicio', 'Estudiar'];

pendientes.forEach((pendientes, index)=> console.log(`${index} : ${pendientes}`))