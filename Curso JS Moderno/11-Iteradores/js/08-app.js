//Ciclo for in: ideal para objetos, ya que en arrays devuelve los índices
const auto = {
    modelo: 'Corolla',
    marca: 'Toyota',
    anio: 2001
}

for(detalles in auto){
    console.log(detalles)
}