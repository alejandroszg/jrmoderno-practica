// Operador && revisa que se cumplen ambas condiciones
const usuario = true;
const puedePagar = true;

if (usuario && puedePagar){
    console.log('Puede pagar')
} else {
    console.log('No puede pagar')
}